Usually people write recursive programs privately, and then write a public method with the same signature (except public) and all it does is just call the private one.
Also try to work with only one array by using a lengthToConsider variable.