## ACADEMIC INTEGRITY RULES
Can talk about code but only in thoughts and strategies.
If you write anything on a whiteboard mothing of that convo can leave the room.
Do not look at anyone else's code, do not share it
DOES NOT APPLY TO TA's
Upperclassmen can see your code but cannot tell you how to do it
Can see assignment code after its due date
Can take a snippet of code but need to credit it and understand it

## DISECTING A HELLO WORLD PROGRAM
A program is a set of instructions you tell a computer to do.
Note to self: put a screenshot of a basic hello world program here.
Public indicates that other files can use this code if they want to
Class indicates that this piece of code is a class, or a collection of data and functionality
The next word is the name of the class. Within the same package (folder), class names need to be unique. This is also the same name as the actual file within your computer storage.
The curly brackets indicate the nested level/grouping. Like a container. Blocks of code.
The "main" function is the one that Java will call first at runtime. Everything that happens when running a program starts in main.
Static means only make one copy
Void means it will not return anything
(String args[]) it is defining what a program expects to receive.
`public static void main(String args[]) {`

Go to the system (os) -> out means it is going to be an output -> println means print a new line
The quotes signal we're starting a String and not instructions. It's just a compilation of characters. Also known as string literal
Semicolons end statements/commands.

## MISC
Code goes from human java language ---> byte code ---> binary. This is compiling
The thing on the left is always the variable and the thing on the right is always the value. It cant be 7 = hours, it needs to be hours = 7.